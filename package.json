{
  "name": "sih26",
  "version": "1.0.0",
  "private": true,
  "workspaces": [
    "src/frontend"
  ],
  "scripts": {
    "build": "npm --prefix src/frontend run build",
    "start": "npm --prefix src/frontend run start"
  }
}
